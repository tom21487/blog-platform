extends basicHTML

block content
  a(href='/control/tags/create' style="display: block") New tag
  | All tags:
  each tag of tags
    div
      | #{tag.name}
      | (
      a(href=`/control/tags/${tag._id}/update`) update
      | ) (
      a(href=`/control/tags/${tag._id}/delete`) delete
      | )
  a(href='/control' style="display: block") Back
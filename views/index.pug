extends layout

block content
  // About showcase
  p#about-text
    if language == "en"
      span.
        Hi, I’m Tom. I like to make stuff with software. I’m a full stack
        developer and I also write algorithms using C/C++. As my resume
        says, I’m a student programmer/engineer.
      a(href='/en/about') More about me
    else if language == "cn"
      span.
        你好，我是Tom。我喜欢用软件创作东西。我是一位全栈开发者，我也用C/C++写算法。
        据我的简历所写，我是一位学生软件工程师。
      a(href='/cn/about') 更多
  div.break

  // Projects showcase
  center
    p
      if language == "en"
        | What I'm working on:
      else if language == "cn"
        | 近期项目：
  each project in projects
    div.quick-view
      div.box
        strong.title
          p
            if language == "en"
              | #{project.titleEn}
            else if language == "cn"
              | #{project.titleCn}
        div.tagspace
          each tag in project.tags
            span.tag
              if tag == "not tagged" && language == "cn"
                | 无标签
              else
                | #{tag}
        img.cover-image(src=project.coverImage)
        p.description
          if language == "en"
            span #{project.descriptionEn}
            a(href=project.urlEn) More
          else if language == "cn"
            span #{project.descriptionCn}
            a(href=project.urlCn) 更多
  else
    p
      if language == "en"
        | No projects!
      else if language == "cn"
        | 没有项目
  center
    a(href='projects.html')
      p
        if language == "en"
          | More projects
        else if langauge == "cn"
          | 更多项目
  div.break
        
  // Blog showcase
  center #[p Latest from the blog:]
  div.quick-view
    div.box
      strong.title #[p Setting up a linux]
      div.tagspace
        span.tag javascript
        span.tag working on
      img.cover-image(src='/images/3.jpg')
      p.description Ubuntu 20.04 is amazing. They've fixed a lot of their...

    div.box
      strong.title #[p Introduction to the MERN stack]
      div.tagspace
        span.tag c++
        span.tag working on
      img.cover-image(src='/images/4.jpg')
      p.description How to get started writing full stack applications.
  center #[a(href='blog.html') #[p More blog posts]]  

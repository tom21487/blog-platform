extends basicHTML

block content
  a(href='/control/tags/create' style="display: block") New tag
  | All tags:
  each tag of tags
    div
      | #{tag.name}
      | (
      a(href=`/control/tags/${tag.name}/update`) update
      | ) (
      a(href=`/control/tags/${tag.name}/delete`) delete
      | )
  a(href='/control/change' style="display: block") Back